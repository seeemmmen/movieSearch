

# 🎬 MovieSearch App (OMDb API)

Приложение для поиска фильмов в реальной базе данных, построенное на **React**. Проект демонстрирует навыки работы с REST API, обработки асинхронных запросов и динамического отображения контента.

---

## 🚀 Особенности

* 🌍 **Реальные данные:** Поиск по огромной базе данных [OMDb](https://www.omdbapi.com/).
* 🔍 **Живой поиск:** Мгновенное отображение результатов при вводе названия.
* 🖼 **Галерея постеров:** Отображение официальных постеров фильмов.
* 💾 **Обработка ошибок:** Уведомления, если фильм не найден или произошла ошибка сети.
* 📱 **Responsive Design:** Полная адаптивность под любые экраны.

---

## 🛠 Стек технологий

* **Frontend:** React (Hooks: `useState`, `useEffect`)
* **API:** [OMDb API](https://www.omdbapi.com/)
* **HTTP-клиент:** Fetch API (или Axios)
* **Styling:** CSS3 / Flexbox / Grid

---

## ⚙️ Установка и запуск

### 1. Получение API Ключа

Для работы приложения вам понадобится бесплатный ключ от OMDb:

1. Перейдите на [omdbapi.com/apikey.aspx](http://www.omdbapi.com/apikey.aspx).
2. Выберите тип "FREE" и зарегистрируйтесь.
3. Подтвердите ключ через почту.

### 2. Клонирование и запуск

```bash
# Клонируйте репозиторий
git clone https://github.com/твой-логин/movieSearch.git

# Перейдите в папку проекта
cd movieSearch

# Установите зависимости
npm install

# Запустите проект
npm start

```

> **Важно:** Не забудьте вставить свой API Key в код (обычно в переменную `API_KEY` в файле `App.js`).

---

## 📂 Структура проекта

```text
src/
├── components/
│   ├── MovieList.js    # Список карточек фильмов
│   ├── MovieCard.js    # Отдельная карточка с данными
│   └── SearchBar.js    # Поле ввода поиска
├── App.js              # Основная логика и Fetch-запросы
└── index.css           # Стилизация приложения

```

---

## 📝 Пример запроса к API

В приложении реализована следующая логика запроса:

```javascript
const searchMovies = async (title) => {
  const response = await fetch(`https://www.omdbapi.com/?apikey=${YOUR_KEY}&s=${title}`);
  const data = await response.json();
  setMovies(data.Search);
};

```

---


---

